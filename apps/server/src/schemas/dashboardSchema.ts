import { z } from 'zod';

// Schema for superadmin dashboard query parameters
export const superadminDashboardSchema = z.object({
  // No specific query parameters for superadmin dashboard
});

// Schema for business partner dashboard query parameters
export const businessPartnerDashboardSchema = z.object({
  // No specific query parameters for business partner dashboard
});

// Schema for franchise dashboard query parameters
export const franchiseDashboardSchema = z.object({
  // No specific query parameters for franchise dashboard
});

// Schema for center dashboard query parameters
export const centerDashboardSchema = z.object({
  // No specific query parameters for center dashboard
});

// Schema for teacher dashboard query parameters
export const teacherDashboardSchema = z.object({
  // No specific query parameters for teacher dashboard
});
